# Phase 3 资源主链路统一 - 2026-06-27 收口验收

本次按“新脚本、新链路、可回退”的原则完成 Phase 3C/3F 的关键收口。

## 已执行脚本

- `sql/90_phase3c_quality_cleanup.sql`

该脚本没有修改已执行过的旧 SQL，而是新增增量脚本，包含审计表 `phase3c_quality_cleanup_audit`，可重复执行。

## 数据治理结果

| 项目 | 处理方式 | 结果 |
| --- | --- | ---: |
| 112 条未挂载资源 | 归入独立治理目录 `/资源治理目录/待归档资源`，不硬塞教材目录 | 112 |
| 7 条孤儿目录资源 | 确定性修复到真实目录节点 2580/2584 | 7 |
| 7 条空文件资源 | 测试/草稿空文件资源软删除，`edu_resource` 与 `resource_main` 同步标记 | 7 |

复跑 `sql/tools/verify_phase3_resource_main_chain.sql` 后：

| 指标 | 当前值 |
| --- | ---: |
| `unplaced_resources` | 0 |
| `empty_file_resources` | 0 |
| `orphan_resources_without_catalog` | 0 |
| `approved_not_published` | 0 |
| `published_but_invisible` | 0 |
| `catalog_nodes_without_resources` | 1376 |

说明：`catalog_nodes_without_resources=1376` 属于全目录覆盖后的空目录现象，不等同于资源脏数据；后续应区分“允许空目录”和“应有资源目录”。

## 后端统一读补口

- `ResourceMainQueryDTO` 增加 `subType`、`brandCode`、`unitName`、`lessonName`、`fileExt`、`catalogNodeId`、`includeSubtree`。
- `VAdminResourceMainMapper.findPage` 支持上述筛选字段。
- 学科资源目录点击已具备通过 `/api/resources/page` 按 `catalogNodeId` 查询的后端条件。

## 前台切流

已迁移到 `resourceGateway`：

- `src/api/resourceGateway.ts`
- `src/composables/useApiResources.ts`
- `src/composables/useBrowseTypeStats.ts`
- `src/composables/useLessonHub.ts`
- `src/composables/useResourceDetail.ts`
- `src/views/resource/SubjectDetailPage.vue` 中的浏览/下载计数与目录预取

保留旧接口仅作为 feature flag 关闭或统一接口失败时的灰度回退。

## 构建验证

- 前端：`npm run build` 通过。
- 后端：`mvn -pl k12-resource -am -DskipTests compile` 通过。
- 数据库：`verify_phase3_resource_main_chain.sql` 通过，核心质量脏数据归零。

## 旧接口下线状态

当前不能直接删除旧接口。原因是 `legacy_api_usage_stat` 近 7 天仍有历史调用：

| api_path | hit_count_7d |
| --- | ---: |
| `/api/primary-chinese/page` | 11 |
| `/api/competition/resources/page` | 9 |
| `/api/topic/resources/page` | 9 |
| `/api/culture/resources/page` | 6 |

下线门槛保持不变：连续观察到核心旧接口调用量为 0 后，再把旧 controller 改为兼容壳或删除旧链路。当前完成的是“前台学科主链路切流 + 观察条件具备”，不是“旧接口物理删除”。
