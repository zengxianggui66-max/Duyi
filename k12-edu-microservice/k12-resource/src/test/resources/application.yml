# 测试环境配置（覆盖 main/resources/application.yml 中与测试无关的项）
spring:
  datasource:
    # ⚠️ 测试前请确认此连接指向 xinketang 数据库
    url: jdbc:mysql://localhost:3306/xinketang?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: zxg123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  cloud:
    nacos:
      discovery:
        enabled: false   # 测试环境关闭 Nacos
  # 关闭 MinIO（测试只写库，不上传文件）

minio:
  endpoint: http://localhost:9000
  accessKey: admin
  secretKey: admin123
  bucketName: qier-duuyi

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: isDeleted
      logic-delete-value: 1
      logic-not-delete-value: 0

jwt:
  secret: k12-education-platform-secret-key-2026-spring-boot-jwt-token
  expiration: 86400000

logging:
  level:
    com.k12.resource.mapper: debug

k12:
  admin:
    rbac:
      enabled: true
